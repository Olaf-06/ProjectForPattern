<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectforpattern">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectForPattern">
        <activity android:name=".MyProducts"
            android:label="Мои продукты"/>
        <activity android:name=".MyPurchase"
            android:label="Мои покупки"/>
        <activity android:name=".EditProfile"
            android:label="Редактор профиля"/>
        <activity android:name=".Basket"
            android:label="Корзина"/>
        <activity android:name=".AddProduct"
            android:label="Добавить продукт"/>
        <activity android:name=".LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>